# IMP-015 implementation plan — runtime-truth parallelism decision for the group-by fallback sort

- Campaign: `tpch-sspq-impl-r1-20260803`
- IMP: `IMP-015` (Performance lane)
- Branch: `impl/tpch-sspq-impl-r1-20260803/IMP-015-gby-sort-runtime-px`
- Base SHA: `607f1ee9fb2394de129e083602c84a6525fc685c` (frozen, IMPL-SSOT §1-c)
- Source spec: `tpch-sspq/impl/IMP-015-implementation-spec.md` (workspace repo, commit `7597a58` amendment included). Decisions D1–D6 are final; this plan lifts spec §B per IMPL-SSOT §5-c after verifying spec §A against the pinned source.
- Upstream scope-check gate (§5-e): **not applicable** — IMP-015 is not in `triage-adjustments.json` `upstream_scope_gates`; the card's §9 records no CBRD issue/PR in the pinned history touching the `SORT_GROUP_BY` branch of `sort_check_parallelism()`.

## §A assumption verification results (verified against this worktree at the base SHA, before any source edit)

| # | Verdict | Evidence |
|---|---|---|
| A1 | **Refuted in the narrow count; formula re-derived, unchanged** | The enum has **three** values: `HS_NONE = 0`, `HS_ACCEPT_ALL`, `HS_REJECT_ALL` (`src/storage/storage_common.h:1240-1245`). `query_dump.c:3944-3951` maps ACCEPT→`hash: true`, REJECT→`hash: partial`, **else**→`hash: false` — the else arm is `HS_NONE`, matching the spec's parenthetical ("`hash: false` = not eligible, not a state"). Runtime `agg_hash_context->state` is initialized `HS_ACCEPT_ALL` in `qexec_alloc_agg_hash_context()` (`query_executor.c:27911`), has exactly **one** assignment to `HS_REJECT_ALL` (`:4845`, the very-high-selectivity test; grep count 1) and is latched (`:4648` early-returns on REJECT). `HS_NONE` is never assigned to `context->state`; it appears only in trace-stats init (`:21239`) and px-scan worker-local init. **Per-state re-derivation of the (a) formula** `plan_eligible && (state != HS_REJECT_ALL)`: ACCEPT → refuse parallelism (hash alive, rationale valid); REJECT → allow (hash abandoned); NONE → refuse — unreachable while `plan_eligible` since alloc sets ACCEPT, and if ever reached the formula degrades to today's behavior (serial), i.e. conservative. Formula stands. |
| A2 | **Holds** | Exactly three `sort_listfile` call sites in `query_executor.c`: `:5571` (partial-hash-list sort, no px arg → serial by `px == NULL`), `:5661` (main gby sort, `gby_px` passed), `:21890` (ANALYTIC). The parallel-scan unconditional-hash path merges worker partial lists into the leader's same `agg_hash_context->part_list_id` (`px_scan_result_handler.cpp:633`) and forces `groupby_stats.groupby_hash = HS_REJECT_ALL` for trace (`:635`); those merged partials are sorted by the same `:5571` call inside `qexec_groupby()`. No third call site exists. |
| A3 | **Holds — stop-and-report NOT triggered** | Parallel `SORT_GROUP_BY` workers read input via `qfile_sort_get_next_parallel` (`external_sort.c:5599`), which "mirrors qfile_make_sort_key()" (`list_file.c:3917`) and builds both the `use_original` (partial) and offset (all-record) `SORT_REC` forms — precisely the inputs of `qfile_compare_partial_sort_record` / `qfile_compare_all_sort_record`. The serial (b) get_fn `qexec_hash_gby_get_next` is itself just `qfile_make_sort_key` over `part_scan_id` (`query_executor.c:4881`), so worker-built records are identical by construction. The put side (`qexec_hash_gby_put_next`) re-fetches original tuples by `SORT_REC` position pointers from `part_list_id->tfile_vfid` (`:4914`) — independent of which get_fn built the record — and in parallel mode `put_fn` runs **serially on the leader** during Phase-3 fan-in (`sort_run_final_single`, `external_sort.c:5677-5683`), preserving the adjacent-equal-key merge semantics (`sorted_count`/`curr_part_key`). The identical machinery with `qfile_compare_partial_sort_record` is already exercised in production by the parallel main group-by sort (`use_original` selection at `:5633-5635`). Load-bearing lifetimes verified: `part_scan_id` is opened before the sort (`:5550`) and `part_list_id` is closed/destroyed only after it (`:5595-5598`). |
| A4 | **Holds — verification criterion 3 downgrades from gate to sanity check (D4)** | `pt_is_hash_agg_eligible` (`src/parser/xasl_generation.c:3883-3901`, applied at `:16555-16568`) sets eligibility 0 for `GROUP_CONCAT`, `MEDIAN`, `CUME_DIST`, `PERCENT_RANK`, `PERCENTILE_CONT/DISC`, and any `DISTINCT` aggregate. Order-sensitive aggregates therefore can never carry plan-time hash eligibility and cannot enter the newly-parallel set {plan hash-eligible ∧ runtime abandonment/spill}. `agg_hash_respect_order` (`query_executor.c:5469`) only gates the emit-directly-from-hash-table shortcut (forcing the sort path so output order matches serial); it never admits an order-sensitive aggregate into eligibility. The order-sensitive suite is still run in §6-b as a sanity check. |
| A5 | **Holds — threshold is conf-tunable** | `parallel_sort_page_threshold` (`src/base/system_parameter.c:5171-5182`; `PRM_FOR_SERVER | PRM_HIDDEN`, default 2048, min 0, max INT_MAX) feeds `compute_parallel_degree()`'s SORT threshold (`px_parallel.cpp:55, 81-83, 117-123`). The D6 TC battery can stay small by lowering it in the TC server's conf. Caveats recorded: (i) the value is read under `std::call_once` into a function-local static (`px_parallel.cpp:47-56`) — a change requires a server restart; (ii) `max_agg_hash_size` is likewise latched into a local static (`query_executor.c:4642`) — same restart rule for the spill-forcing TC; (iii) real parallelism additionally requires `parallelism ≥ 2`, `input page_cnt ≥ threshold`, `tuple_cnt ≥ degree`, `page_cnt ≥ degree` (`external_sort.c:5240`) and a successful `try_reserve_workers()`. TC arming constants confirmed: `HASH_AGGREGATE_VH_SELECTIVITY_TUPLE_THRESHOLD = 2000`, `HASH_AGGREGATE_VH_SELECTIVITY_THRESHOLD = 0.5f` (`query_executor.c:131,134`) — matching D6's "> 2000 tuples, > 50% distinct groups". TC conf overrides are confined to the TC/correctness server run; every measurement block keeps the pinned `cubrid.conf` (sha `ad19f5ac…`, §6-a-2, zero deviation). |

## §5-c items 1–10

1. **Hypothesis (falsifiable).** The serial group-by fallback sort under `hash: partial` is caused solely by the plan-time `hash_eligible` flag reaching `sort_check_parallelism()`; substituting per-call-site runtime truth parallelizes it, moving Q10 native median wall to ≤ 5.70 s (measured control 5.426 s + 1.22% block-spread margin) with byte-identical results.
2. **CUBRID `file:line` to change.** `src/query/query_executor.c:5657` — the px flag formula becomes `gbstate.hash_eligible && gbstate.agg_hash_context->state != HS_REJECT_ALL` (change (a)); `src/query/query_executor.c:5571-5573` — construct a `SORT_LISTFILE_PX_ARG` (`key_info = &agg_hash_context->sort_key`, `input_list = agg_hash_context->part_list_id`, `hash_eligible = 0` unconditionally per D2, `stats = &xasl->groupby_stats`, `parallelism = xasl->parallelism`) and pass it (change (b)). **Unchanged but load-bearing:** `external_sort.c:5228-5234` (`px == NULL || px->hash_eligible` refusal), `external_sort.h:167-174` (px arg contract), `query_executor.c:4845/:4648/:4862`, `query_dump.c:3944-3951`, ANALYTIC precedent `query_executor.c:21883-21890`.
3. **PostgreSQL reference (pinned `5713b437`).** `planner.c:8019/8083/7793/7588` (gather_grouping_paths: sort parallelism independent of hashing); `nodeAgg.c:1866/1907` (hash memory-budget breach ⇒ spill mode, never a strategy flip or a parallelism veto).
4. **Changed files / LOC band.** 1 file (`src/query/query_executor.c`). LOC low 2 / likely 8 / high 25 ((b)'s px construction dominates). §5-d hard stop at 150% of high = **38 LOC**, or any unanticipated subsystem.
5. **Expected metric signature.** Q10 native trace: `GROUPBY (... hash: partial, sort: true ...)` gains a `(parallel workers: N ≥ 2)` sub-line; group-by sort ioread → ~0; TWU 3.42875 → ≥ 3.70. Q18: `qfile_compare_partial_sort_record` profile share (3.27%) drops; 1-unit phase share (63.4%) shrinks — observation, not a gate. Direction of all §6-c capture-list counters recorded.
6. **Correctness risk & testing.** One real hazard: input-order sensitivity (D4/A4 — A4 verified holds, so criterion 3 is a sanity check). Tests: the five mandatory §6-b checks, where check 1 is the D6 two-layer battery — SQL correctness TCs (high-selectivity `HS_REJECT_ALL` arming; tiny `max_agg_hash_size` spill arming; order-sensitive control `GROUP_CONCAT` + order-dependent MIN/MAX under `agg_hash_respect_order=y`) compared for identical results against the serial reference (`/*+ NO_HASH_AGGREGATE */` and/or parallelism off), plus the shell arming-proof TC (trace must show `hash: partial` **and** `parallel workers: N >= 2`; for the (b) case the spill-sort statement gains one) — both housed in the campaign harness only; Q10 20-row byte-identity under the section-11 comparator.
7. **Target queries.** Q10 (primary gate, measured 1.313675x route); Q15 (armed, lower-bound corroboration); Q18 (armed, upper-bound observation only — no gate, no isolating A/B exists).
8. **Negative controls (must not change).** Q11 (null by size: 1,516-page sort < 2048-page threshold, `compute_parallel_degree` returns 0 regardless — must stay null); Q16 (GROUPBY already parallel; its 1.041 s post-sort serial loop belongs to IMP-023 and must not move); Q01/Q03/Q05 group-by wall and plan shape; plan shapes campaign-wide (estimated costs unchanged — executor-only change).
9. **Overlap / dependency.** IMP-016: genuine ALTERNATIVE for Q10's 1.313675x — at most one is counted; IMP-015 is the measured route. IMP-017: predecessor of IMP-016's memory arm, orthogonal here. IMP-021: anti-additive (removes the sort this parallelizes). IMP-023: separate post-sort consumer loop. IC-5 (merge/finalize parallelization): strictly sequenced after (D3); consumes IMP-015's re-measured baseline. `agg_hash_respect_order` parallel-time policy: deferred to IC-5 (D5).
10. **Rollback.** Single revert of the candidate-branch patch commit; no XASL serialization, persistent format, or lock-protocol surface (any such contact triggers §5-d stop-and-report).

## Verification-criteria disposition (card §5, via spec §C)

| Criterion | Disposition |
|---|---|
| 1. Q10 trace parallel sub-line + ioread ~0 + byte-identical 20 rows | gate |
| 2. Q10 median ≤ 5.70 s, U ≥ 3.70 | gate (paired B→P→P→B per §6-c/6-d, corrected MDE per §6-d-1) |
| 3. order-sensitive aggregate suite | sanity check (A4 verified holds at source) |
| 4. Q01/Q03/Q05 no regression, plan shapes unchanged | gate |
| 5. worker-pool pressure | no design item — existing `try_reserve_workers()` admission; detector is the standard Q01–Q22 stream wall no-regression; degradation to degree 1 is the built-in relief |
