# P0.1 — Source Coordinate Re-Grounding

- **Task**: P0.1 (re-ground ralplan.md §9 Source Coordinates + spec.md "Technical Context" coordinates)
- **Base SHA**: `ebf08d0803487c1a8c4b35850b3857050ca67783` (branch `feature/value-handle-stage1`, worktree `/home/cubrid/dev/cubrid-vh`), confirmed via `.git/worktrees/cubrid-vh/HEAD` → `ref: refs/heads/feature/value-handle-stage1` → `refs/heads/feature/value-handle-stage1` = `ebf08d0803487c1a8c4b35850b3857050ca67783`.
- **Prior base**: ralplan.md §9 was verified at `659dccd67`. This document re-grounds every one of those coordinates plus every spec.md §"Technical Context" coordinate at `ebf08d080`.
- **Date**: 2026-07-09
- **Method**: `search` for each symbol/macro/struct in the worktree source tree, followed by targeted `read` of the surrounding lines to confirm the code at the new coordinate still matches the semantics the plan attributes to it. Cross-list copy-site line numbers and the two peer-owned instrumented files (`heap_file.c`, `list_file.c`) were additionally cross-checked live via IRC with `1-P02-ListClassAudit` and `3-P03-Instrumentation`, who confirmed the same coordinates via `git show ebf08d080:<path>` independently (see "Live-worktree caveat" below). No source files were edited by this task.

## Live-worktree caveat (read this before trusting heap_file.c / list_file.c line numbers)

During this audit, `3-P03-Instrumentation` (sanctioned editor, per `0-Main`) landed **uncommitted, `#if !defined(NDEBUG)`-guarded temporary VH-P0-TEMP-COUNTER debug-counter blocks** into `src/storage/heap_file.c` (+~47 lines before `heap_attrvalue_transform_to_dbvalue`, ~10402/10478 post-edit signature lines for `heap_attrvalue_point_fixed`/`heap_attrvalue_transform_to_dbvalue` vs. committed 10366/10431) and `src/query/list_file.c` (+84 lines total: one block near `qfile_add_tuple_to_list_id` (~line 1595) plus one-line inserts in `qfile_open_list` (~1247) and `qfile_destroy_list` (~2272)). These are **purely additive, NDEBUG-guarded, no semantic change to release-build behavior**, and are expected to be removed before P3 (P0.3 §ralplan). All coordinates below are re-grounded against the **git-committed content at `ebf08d080`** (i.e., pre-instrumentation), confirmed by direct `search`/`read` captured before the edits landed in the live tree, and cross-confirmed by `1-P02-ListClassAudit` and `3-P03-Instrumentation` via `git show ebf08d080:<path>`. No coordinate below is contaminated by the live overlay.

## Part 1 — ralplan.md §9 "Source Coordinates" (verified at `659dccd67` → re-grounded at `ebf08d080`)

| # | Reference | Old (659dccd67) | New (ebf08d080) | Status | Confirmation |
|---|---|---|---|---|---|
| 1 | `heap_attrvalue_point_fixed` | heap_file.c:10366 (static) | heap_file.c:10366 (static) | **VERIFIED** | `static void heap_attrvalue_point_fixed (RECDES * recdes, HEAP_CACHE_ATTRINFO * attr_info, OR_ATTRIBUTE * attrepr, RECDES * raw)` |
| 2 | `heap_attrvalue_transform_to_dbvalue` | heap_file.c:10431 (static) | heap_file.c:10431 (static) | **VERIFIED** | `static int heap_attrvalue_transform_to_dbvalue (HEAP_ATTRVALUE * value, OR_ATTRIBUTE * attrepr, RECDES * raw)` |
| 3 | `QFILE_TUPLE_PREV_LENGTH_OFFSET` | query_list.h:231 | query_list.h:231 | **VERIFIED** | `#define QFILE_TUPLE_PREV_LENGTH_OFFSET          4` |
| 4 | `QFILE_GET_PREV_TUPLE_LENGTH` read — list_file.c | list_file.c:5212 | list_file.c:5212 | **VERIFIED** | `scan_id_p->curr_offset -= QFILE_GET_PREV_TUPLE_LENGTH (scan_id_p->curr_tpl);` (in `qfile_scan_prev`) |
| 5 | `QFILE_GET_PREV_TUPLE_LENGTH` read — cursor.c | cursor.c:1590 | cursor.c:1590 | **VERIFIED** | `cursor_id_p->current_tuple_offset -= QFILE_GET_PREV_TUPLE_LENGTH (cursor_id_p->current_tuple_p);` (in `cursor_prev_tuple`); file is at `src/query/cursor.c` |
| 6 | `QFILE_PUT_PREV_TUPLE_LENGTH` write | list_file.c:1597 | list_file.c:1597 | **VERIFIED** | `QFILE_PUT_PREV_TUPLE_LENGTH (page_p, list_id_p->lasttpl_len);` |
| 7 | `f_data_cmpdisk` / `f_index_cmpdisk` | object_primitive.h:108-110 | object_primitive.h:108-110 | **VERIFIED** | `f_index_cmpdisk` field at 108, `f_data_cmpdisk` field at 110, inside `pr_type` class |
| 8 | `PGBUF_IS_PAGE_CHANGED` + `restart_scan_oid` | scan_manager.c:5986-5991 | scan_manager.c:5986-5991 | **VERIFIED** | `if (is_peeking == PEEK && ... && PGBUF_IS_PAGE_CHANGED (...)) { is_peeking = COPY; COPY_OID(...); goto restart_scan_oid; }` — **note**: the identical pattern repeats 4 more times in the same function at 6164, 6185, 6202, 6223 (not previously enumerated; relevant to D-G2's "bump-point 4종 중 3번 소멸" descope — the demotion pattern is more pervasive than one citation suggests) |
| 9 | PEEK→COPY demotion (parallel) | px_scan_slot_iterator.cpp:131-136,145-150,159-164 | px_scan_slot_iterator.cpp:131-136,145-150,159-164 | **VERIFIED** | All 3 ranges match exactly: 131-136 (`if (is_peeking==PEEK && ...) {...goto restart_scan_oid;}`), 145-150 (same pattern, `m_rest_attr_cache` branch), 159-164 (same pattern, after `fetch_val_list` PEEK call). File lives at `src/query/parallel/px_scan/px_scan_slot_iterator.cpp` |
| 10 | `qmgr_free_old_page_and_init` (page transition) | list_file.c:5234 | list_file.c:**5233** | **DRIFTED: 5234→5233** | `qmgr_free_old_page_and_init (thread_p, scan_id_p->curr_pgptr, scan_id_p->list_id.tfile_vfid);` immediately precedes `QFILE_COPY_VPID (&scan_id_p->curr_vpid, &prev_vpid);` (now at 5234) inside `qfile_scan_prev` — same function/semantics, 1-line off. |
| 11 | `QFILE_OVERFLOW_TUPLE_COUNT_FLAG` | query_list.h:280 | query_list.h:280 | **VERIFIED** | `#define QFILE_OVERFLOW_TUPLE_COUNT_FLAG -2` |
| 12 | `cursor.c` in client library | cs/CMakeLists.txt:263 | cs/CMakeLists.txt:263 | **VERIFIED** | `${QUERY_DIR}/cursor.c` |
| 13 | `QFILE_LIST_CACHE` definition | list_file.c:106-180 | list_file.c:106-180 | **VERIFIED** | `typedef struct qfile_list_cache QFILE_LIST_CACHE;` at 106; range covers struct + `QFILE_LIST_CACHE_CANDIDATE` + static instances through 180. Confirmed pre-instrumentation (P03's inserts are all below line 1200, do not shift this) |
| 14 | `qfile_update_list_cache_entry` declared | list_file.h:198 | list_file.h:198 | **VERIFIED** | `QFILE_LIST_CACHE_ENTRY *qfile_update_list_cache_entry (THREAD_ENTRY * thread_p, int list_ht_no, ...` |
| 15 | `qfile_update_list_cache_entry` implemented | list_file.c:6530 | list_file.c:6530 | **VERIFIED** | `qfile_update_list_cache_entry (THREAD_ENTRY * thread_p, int list_ht_no, const DB_VALUE_ARRAY * params, ...)` — confirmed pre-instrumentation with `3-P03-Instrumentation` (their edits are above this point in the file, shift it by a constant +84 in the live overlay but not in the committed tree) |
| 16 | `qfile_scan_list_prev` callers — query_executor.c | 6142,23533,23567 | 6142,23533,23567 | **VERIFIED** (all 3) | 6142: `pre##_scan = qfile_scan_list_prev((thread_p), &(pre##_sid), ...)` (macro); 23533/23567: `sc = qfile_scan_list_prev (thread_p, &func_state->{value,group}_scan_id, ...)` |
| 17 | `qfile_scan_list_prev` caller — scan_manager.c | 7863 | 7863 | **VERIFIED** | `while ((qp_scan = qfile_scan_list_prev (thread_p, &llsidp->lsid, &tplrec, PEEK)) == S_SUCCESS)` |
| 18 | `qfile_scan_list_prev` caller — list_file.c (sort-key backtrack) | 3632 | 3632 | **VERIFIED*** | `scan_status = qfile_scan_prev (thread_p, input_scan_p);` inside sort-record-doesn't-fit backtrack path. **Note**: this call site invokes the internal static callback `qfile_scan_prev` directly (the function `qfile_scan_list_prev` itself dispatches to via `qfile_scan_list(..., qfile_scan_prev, ...)`), not the public `qfile_scan_list_prev` wrapper — same as the original grouping implies (both ultimately trigger the same page-release backward-scan machinery); flagging for precision, not a drift |
| 19 | `qfile_copy_tuple` | list_file.c:3306 (static) | list_file.c:3306 (static) | **VERIFIED** | `static int qfile_copy_tuple (THREAD_ENTRY * thread_p, QFILE_LIST_ID * to_list_id_p, QFILE_LIST_ID * from_list_id_p)` — independently confirmed by `1-P02-ListClassAudit` via `git show ebf08d080` |
| 20 | `qfile_combine_two_list` | list_file.c:2691 | list_file.c:2691 | **VERIFIED** | `qfile_combine_two_list (THREAD_ENTRY * thread_p, QFILE_LIST_ID * lhs_file_p, QFILE_LIST_ID * rhs_file_p, int flag)` — independently confirmed by P02 |
| 21 | `qfile_union_list` | list_file.c:3362 (static) | list_file.c:3362 (static) | **VERIFIED** | `static QFILE_LIST_ID * qfile_union_list (THREAD_ENTRY * thread_p, QFILE_LIST_ID * list_id1_p, QFILE_LIST_ID * list_id2_p, int flag)` — independently confirmed by P02 |
| 22 | `qfile_duplicate_list` | list_file.c:4933 | list_file.c:4933 | **VERIFIED** | `QFILE_LIST_ID * qfile_duplicate_list (THREAD_ENTRY * thread_p, QFILE_LIST_ID * list_id_p, int flag)` — independently confirmed by P02 |
| 23 | `qfile_copy_list_pages` | list_file.c:4816 (static) | list_file.c:4816 (static) | **VERIFIED** | `static int qfile_copy_list_pages (THREAD_ENTRY * thread_p, VPID * old_first_vpid_p, ...)` — independently confirmed by P02 |
| 24 | Executable output dir (`CMAKE_RUNTIME_OUTPUT_DIRECTORY`) | CMakeLists.txt:67 | CMakeLists.txt:67 | **VERIFIED** | `set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin)` |
| 25 | String compression threshold (`OR_MINIMUM_STRING_LENGTH_FOR_COMPRESSION`) | object_representation.h:1384 | object_representation.h:1384 | **VERIFIED** | `#define OR_MINIMUM_STRING_LENGTH_FOR_COMPRESSION 255` |
| 26 | Compressed varchar per-cmp alloc | object_primitive.c:11931-11952 | object_primitive.c:11931-11952 | **VERIFIED** | `or_init (&tmp_buf, data1, cmp_size1); rc = pr_get_compressed_data_from_buffer (&tmp_buf, decompressed1, ...)` (side 1) through the equivalent side-2 call at 11952, inside `mr_cmpdisk_char_type_common` |
| 27 | NUMERIC cmpdisk double-readval | object_primitive.c:8838-8863 | object_primitive.c:8838-8863 | **VERIFIED** | `mr_index_cmpdisk_numeric` (8838) → `mr_data_cmpdisk_numeric` first `mr_data_readval_numeric` call at 8858 + error check through 8863; **note**: the *second* readval call (for `mem2`) is at line 8865, just past the cited range end — the "double-readval" pattern spans 8838-8880 in full, the 8838-8863 sub-citation only fully covers the first of the two calls (unchanged from original citation, not a new drift) |
| 28 | `QFILE_LIST_SCAN_ID.tplrec` (overflow peek) | query_list.h:514 | query_list.h:514 | **VERIFIED** | `QFILE_TUPLE_RECORD tplrec;	/* used for overflow tuple peeking */` |
| 29 | Disk-direct sort compare | list_file.c:4505 | list_file.c:4505 | **VERIFIED** | `subkey->sort_f = types->domp[i]->type->get_data_cmpdisk_function ();` |

## Part 2 — spec.md "Technical Context" coordinates (first-time grounding at `ebf08d080`)

| # | Reference | Cited coordinate | Status | New coordinate | Confirmation |
|---|---|---|---|---|---|
| 30 | DB_VALUE struct | dbtype_def.h:1166 | **VERIFIED** | dbtype_def.h:1166 | `struct db_value { DB_DOMAIN_INFO domain; DB_DATA data; ...` (typedef at 1165) |
| 31 | need_clear / compressed_string cache fields | dbtype_def.h:1085-1087 | **DRIFTED: 1085-1087→1084-1088** | dbtype_def.h:1084-1088 | `compressed_need_clear` (the "need_clear" field) is at **1084**, `size`/`buf` at 1085-1086, `compressed_buf`/`compressed_size` (the "compressed_string" cache) at **1087-1088** — inside the `medium` sub-struct of the `DB_DATA` union's char/varchar member. Cited range (1085-1087) captures `size`/`buf`/`compressed_buf` but misses `compressed_need_clear` (1084) at the start and `compressed_size` (1088) at the end |
| 32 | temp list-file overhead (8B length + 8B per-value header) | query_list.h:229-236 | **VERIFIED** | query_list.h:229-236 | `QFILE_TUPLE_LENGTH_SIZE`(229)/`_OFFSET`(230), `QFILE_TUPLE_PREV_LENGTH_OFFSET`(231), `QFILE_TUPLE_VALUE_HEADER_LENGTH`/`_SIZE`(233-234), `QFILE_TUPLE_VALUE_FLAG_SIZE`/`_LENGTH_SIZE`(235-236) |
| 33 | `f_data_cmpdisk`/`f_index_cmpdisk` infra (pr_type class body) | object_primitive.h:73-180 | **VERIFIED** | object_primitive.h:73-180 | `typedef DB_VALUE_COMPARE_RESULT (*index_cmpdisk_function_type)...` at 73 through `} PR_TYPE, *PRIM;` at 180 — full `pr_type` class definition |
| 34a | Materialization pt.1: `heap_attrvalue_transform_to_dbvalue` body | heap_file.c:10431-10477 | **VERIFIED** | heap_file.c:10431-10477 (pre-instrumentation) | Function body from signature (10431) through `rv = pr_type->data_readval(...)` / domain-init fallback logic; committed length matches (back-calculated from live-overlay: current signature 10478 minus the peer's 47-line pre-shift = 10431; current close-brace 10537 minus the function's own 12-line inserted counter block = 10525, i.e. body spans ~10431-10477/10478 pre-edit — matches cited end almost exactly, effectively unchanged) |
| 34b | Materialization pt.1: `heap_attrvalue_point_fixed` call site (fixed-length pointer-then-copy) | heap_file.c:10524 | **DRIFTED: 10524→10523** | heap_file.c:10523 (pre-instrumentation) | `heap_attrvalue_point_fixed (recdes, attr_info, attrepr, &raw);` inside `heap_attrvalue_read`, immediately before the `return heap_attrvalue_transform_to_dbvalue(...)` call at 10532 |
| 35 | Materialization pt.2: `qdata_copy_db_value_to_tuple_value` body | query_opfunc.c:376-401 | **VERIFIED** | query_opfunc.c:376-401 | `pr_type = pr_type_from_id (dbval_type);` (376) through end of `data_writeval`/align logic just before the `#if !defined(NDEBUG)` valgrind-suppress block (401→402); function itself starts at 356 |
| 36 | Materialization pt.2: fast path call site | list_file.c:1705 | **DRIFTED: 1705→1704** | list_file.c:1704 | `if (qdata_copy_db_value_to_tuple_value (tuple_descr_p->f_valp[i], tuple_p, &tuple_value_size) != NO_ERROR)` inside `qfile_save_normal_tuple`; cited line 1705 is now the opening `{` of that if-block |
| 37 | Materialization pt.2: slow path call site | query_executor.c:1277 | **VERIFIED** | query_executor.c:1277 | `if ((qdata_copy_valptr_list_to_tuple (thread_p, xasl->outptr_list, &xasl_state->vd, tplrec) != NO_ERROR))` |
| 38 | Materialization pt.3: `fetch.c` TYPE_POSITION peek-read | fetch.c:4049-4061 | **VERIFIED** | fetch.c:4049-4061 | `if (flag == V_BOUND) { pr_type = ...->dom->type; ...; or_init(&buf, ptr, length); if (pr_type->data_readval(&buf, *peek_dbval, ..., false /* Don't copy */, ...) != NO_ERROR) {` — `case TYPE_POSITION:` itself is at 4039 |
| 39 | Materialization pt.3: `fetch.c` TYPE_POSITION multi-value fetch | fetch.c:4654 | **VERIFIED** | fetch.c:4654 | `if (pr_type->data_readval (&buf, regu_var->vfetch_to, regu_var->domain, -1, false /* Don't copy */, NULL, 0) != NO_ERROR)` — the `assert_release (regu_var->type == TYPE_POSITION);` guard for this loop is at 4645 |
| 40 | GROUP BY input fetch (peek) | query_executor.c:4584 | **VERIFIED** | query_executor.c:4584 | `if (fetch_val_list (thread_p, gbstate->g_regu_list, &gbstate->xasl_state->vd, NULL, NULL, tpl, peek) != NO_ERROR)` |
| 41 | analytic input fetch (peek) | query_executor.c:22794 | **VERIFIED** | query_executor.c:22794 | `if (fetch_val_list (thread_p, analytic_state->a_regu_list, &xasl_state->vd, NULL, NULL, tpl, peek) != NO_ERROR)` |
| 42 | Sort disk-direct compare (partial-key) | list_file.c:4232-4310 | **VERIFIED** | list_file.c:4232-4310 | `qfile_compare_partial_sort_record (const void *pk0, const void *pk1, void *arg)` (4232) through the return + start of `qfile_compare_all_sort_record` signature at 4310; uses `(*key_info_p->key[i].sort_f)` disk-direct compare, never materializes a `DB_VALUE` |
| 43 | Sort disk-direct compare (cmpdisk-function assignment) | list_file.c:4505-4536 | **VERIFIED** | list_file.c:4505-4536 | `subkey->sort_f = ...->get_data_cmpdisk_function ();` assigned per-column at 4505/4509, and again in the fallback (non-permuted) loop through 4536 |
| 44 | Merge/distinct materialization | list_file.c:807-873 | **VERIFIED** | list_file.c:807-873 | `qfile_compare_tuple_values (QFILE_TUPLE tuple1, QFILE_TUPLE tuple2, ...)` (807) — the *contrasting* case to #42/#43: this one **does** call `pr_type_p->data_readval(...)` to materialize `DB_VALUE dbval1/dbval2` before `pr_type_p->cmpval(...)`, through `pr_clear_value(&dbval2);` (873) |
| 45 | Compressed varchar: peek forced alloc | object_primitive.c:11807-11851 | **VERIFIED** | object_primitive.c:11807-11851 | `if (copy_buf && copy_buf_len >= expected_decompressed_size + 1) {...} else { decompressed_string = db_private_alloc(...) }` through the compressed-string caching path (`#if defined(CS_MODE) ... #else ... db_set_compressed_string(...) #endif`) closing at 11851 |
| 46 | Compressed varchar: per-cmp malloc+decompress | object_primitive.c:11897-11960 | **VERIFIED** | object_primitive.c:11897-11960 | `mr_cmpdisk_char_type_common (void *mem1, void *mem2, ...)` (11897) through both sides' `db_private_alloc` + `pr_get_compressed_data_from_buffer` decompress blocks (ending 11960) |
| 47 | NUMERIC (3B header + truncated buffer, readval) | object_primitive.c:8706-8880 | **VERIFIED (exact)** | object_primitive.c:8706-8880 | `mr_data_writeval_numeric` (8706) through `mr_data_cmpdisk_numeric`'s closing `return c; }` (8880) — range boundaries match to the line |
| 48 | Byte order macros (short/int/int64/bigint/float/double) | object_representation.h:102-131 | **VERIFIED** | object_representation.h:102-131 | `OR_PUT_SHORT`(102, `htons`) through `OR_GET_BIGINT`(131, via `swap64`) |
| 49 | Byte order macros (extended: time/utime/timestamptz/date/datetime) | object_representation.h:171-210 | **VERIFIED** | object_representation.h:171-210 | `OR_PUT_TIME`(170)/`OR_GET_TIME`(173) through `OR_GET_DATETIME`(206-210) |
| 50 | `byte_order.c` `htonf`/`htond` | byte_order.c:159-199 | **VERIFIED** (path clarified) | src/storage/byte_order.c:159-199 | `htonf (float from)` at 159, `htond (double from)` at 178, both `#if !defined(OR_HAVE_HTONF/HTOND)`-guarded, closing `#endif /* OR_BYTE_ORDER == OR_LITTLE_ENDIAN */` at exactly line 199. **File path note**: actual location is `src/storage/byte_order.c`, not `src/base/` (no `byte_order.c` exists under `src/base/`) |
| 51 | Lifetime risk: fixed_scan deadlock decision | query_executor.c:16146-16224 | **VERIFIED (exact)** | query_executor.c:16146-16224 | `if (xasl->spec_list) {` (16146) — "may lead to page deadlocks" comment at 16151 — through the last `aptr_list` `XASL_LINK_TO_REGU_VARIABLE` → `fixed_scan_xasl = NULL;` loop closing at 16224 |
| 52 | Lifetime risk: `PGBUF_IS_PAGE_CHANGED` COPY demotion | scan_manager.c:5986-5991 | **VERIFIED** | scan_manager.c:5986-5991 | Same coordinate as Part 1, row 8 — cited independently by both ralplan §9 and spec.md Technical Context |
| 53 | Lifetime risk: next/jump page release + overflow copy | list_file.c:5157-5422 | **VERIFIED** | list_file.c:5157-5422 | Range spans `qfile_scan_next`'s forward page-release (5157, `qmgr_free_old_page_and_init` before advancing `curr_pgptr`) through `qfile_scan_by_position`'s page-release-on-S_ON transition (5422) — covers both next/jump page free and the overflow-tuple page-copy machinery in between |

## Summary

- **Total coordinates re-grounded**: 53 (29 from ralplan.md §9 + 24 from spec.md Technical Context, with row 52 an intentional cross-reference duplicate of row 8)
- **VERIFIED (exact, same line/range)**: 48
- **DRIFTED (line moved, semantics unchanged)**: 4 — all off-by-one, all same-function/same-semantics:
  - Row 10: `qmgr_free_old_page_and_init` (page transition) list_file.c 5234→**5233**
  - Row 31: dbtype_def.h need_clear/compressed_string cache fields 1085-1087→**1084-1088** (range was one line short at both ends)
  - Row 34b: `heap_attrvalue_point_fixed` call site heap_file.c 10524→**10523**
  - Row 36: `qdata_copy_db_value_to_tuple_value` fast-path call site list_file.c 1705→**1704**
- **Zero unresolved entries.**
- **Semantic changes (not just line drift)**: **none found**. Every drifted or verified coordinate's surrounding code matches the behavior ralplan/spec attribute to it — no logic changes, no removed/renamed functions, no signature changes. The only actual *content* changes present anywhere in the two hottest files (`heap_file.c`, `list_file.c`) are the peer-owned, `#if !defined(NDEBUG)`-guarded, uncommitted P0.3/P0.2 temporary debug-counter insertions described in the "Live-worktree caveat" above — these are additive-only, invisible in release builds, and don't touch any cited symbol's logic.
- **Noteworthy findings for downstream P0/P1 tasks** (flagged prominently per instructions, not drift):
  1. The `PGBUF_IS_PAGE_CHANGED` + `restart_scan_oid` PEEK→COPY demotion pattern (row 8) recurs **5 times** within the same heap-scan function (5986, 6164, 6185, 6202, 6223), not once — relevant to D-G2's claim that this demotion-retry bump-point class disappears from the main line; P1 should confirm all 5 occurrences are covered by the descope, not just the cited one.
  2. `list_file.c:3632` (row 18) calls the internal static `qfile_scan_prev` callback directly rather than through the public `qfile_scan_list_prev` wrapper — same effective backward-scan/page-release path, but P0.2's caller-classification table (owned by `1-P02-ListClassAudit`) should record this distinction for callgraph precision.
  3. NUMERIC cmpdisk's "double-readval" citation (row 27, object_primitive.c:8838-8863) only fully encloses the *first* of the two `mr_data_readval_numeric` calls in `mr_data_cmpdisk_numeric`; the second call is at line 8865, just past the cited range (full double-readval pattern is object_primitive.c:8838-8880, i.e. row 47's superset range).

## Blockers

None. All 53 coordinates resolved with zero unresolved entries.
